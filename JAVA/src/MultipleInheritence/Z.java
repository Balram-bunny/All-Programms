package MultipleInheritence;

public interface Z extends Y,X {  /* class can extend only one class, but interface can extend more than one interface */
	                                
 void m3();
}
